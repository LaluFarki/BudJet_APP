name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  curved_navigation_bar: ^1.0.6
  flutter:
    sdk: flutter
  intl: ^0.19.0
  get: ^4.6.6              # State management & navigasi
  firebase_core: ^3.15.2    # Koneksi dasar Firebase
  cloud_firestore: ^5.6.12  # Database untuk simpan transaksi
  csv: ^6.0.0              # Untuk fitur download CSV (Target 10)
  flutter_slidable: ^3.1.1
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  firebase_auth: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

test: ^1.24.0

flutter:
  uses-material-design: true
  assets:
    - assets/
