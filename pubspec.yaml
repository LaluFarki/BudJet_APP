name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  curved_navigation_bar: ^1.0.6
  flutter:
    sdk: flutter
  intl: ^0.19.0
  get: ^4.6.6              # State management & navigasi
  firebase_core: ^3.0.0    # Koneksi dasar Firebase
  cloud_firestore: ^5.0.0  # Database untuk simpan transaksi
  csv: ^6.0.0              # Untuk fitur download CSV (Target 10)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

test: ^1.24.0

flutter:
  uses-material-design: true
